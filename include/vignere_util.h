#ifndef _VIGNERE_UTIL_H
#define _VIGNERE_UTIL_H

std::string uppercase_form_of(const std::string&);
bool is_only_alphabets_in(const std::string&);
int row_of(char);
int column_of(char);
char alphabet_at(int);

#endif
